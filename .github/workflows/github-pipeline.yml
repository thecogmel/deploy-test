name: Deploy to DigitalOcean Droplet

on:
  push:
    branches:
      - main  # Ou sua branch de preferência

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.5.3
        with:
          key: ${{ secrets.SSH_KEY }}
      
      - name: Deploy to Digital Ocean
        run: |
          ssh -o StrictHostKeyChecking=no root@24.199.118.55 "cd / && git pull && your deployment commands"
